import { Routes } from '@angular/router';
import { StatCalculatorComponent } from './components/stat-calculator/stat-calculator.component';

export const routes: Routes = [
  { path: '', component: StatCalculatorComponent }
];